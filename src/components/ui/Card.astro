---
interface Props {
	class?: string;
	[key: string]: any;
}

const { class: className = '', ...rest } = Astro.props;
---

<div class={`rounded-2xl bg-card border border-border shadow-sm ${className}`} {...rest}>
	<slot />
</div>
